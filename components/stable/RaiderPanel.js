import Grid from '@material-ui/core/Grid';

const RaiderPanel = (props) => {
    console.log(props);
    return (
        <Grid container spacing={1}>

        </Grid>
    )
}

module.exports = RaiderPanel;